export type GetDigitsConfigType = {
  days: string;
  hasDays: string | null;
  hasHours: string | null;
  hasMinutes: string | null;
  hasSeconds: string | null;
  hours: string;
  minutes: string;
  seconds: string;
};

export type DigitsConfigTupleType = [string, string, string];
